# Search User Tool - Consuming GitHub API

This is a project that uses a API to search the data of any user on GitHub. It's showed name, profile picture, bio, followers, following, repositories & events.